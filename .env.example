# Required for reading recommendations (recommend/search.py).
# Every other stage works fully offline.
TAVILY_API_KEY=

# Optional: only needed if Ollama is not running on its default host/port.
# OLLAMA_HOST=http://127.0.0.1:11434
